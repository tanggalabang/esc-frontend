export function isAdmin(req: any, res: any, next: any) {
  // const user = req.user; // Anda perlu mengatur req.user saat pengguna masuk
  // if (user && user.user_type === 1) {
  //   return next();
  // } else {
  //   return res.status(403).send('Forbidden'); // Ganti ini dengan cara yang sesuai dengan aplikasi Anda
  // }
}
